<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.blue.corner.mapper.LikeMapper">
    <insert id="addLike">
        insert into `like` (id, user_id, article_id, status)
        values (#{id},#{userId},#{articleId},#{status});
    </insert>
</mapper>